# Codex Bundle

This folder mirrors the full Codex-ready bundle from the repository root.

## Notes

- Keep `SKILL.md` and `automation.toml` as the source of truth.
- All paths inside the bundle should stay portable and machine-agnostic.
- This folder is the Codex-side mirror of the repository's public assets.
